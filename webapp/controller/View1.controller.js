sap.ui.define([
	"sap/ui/core/mvc/Controller",
	"sap/m/MessageToast",
	"sap/m/MessageBox"
], function (Controller, MessageToast, MessageBox) {
	"use strict";
	return Controller.extend("ovly.controles.eventos.controller.View1", {
		onInit: function () {},
		onPressToast: function (oEvt) {
			// console.log(oEvt.getSource().getText());
			this._consolePrint(oEvt.getSource(), "text");
			MessageToast.show("Apertou"); // sap.m.MessageToast.show("Apertou");
		},
		onPressSucesso: function (oEvt) {
			// console.log(oEvt.getSource().getText());
			this._consolePrint(oEvt.getSource(), "text");
			MessageBox.success("Sucesso!!!");
		},
		onChangeRating: function (oEvt) {
			// debugger;
			var mSettings = {
				title: "Nota"
			};
			// var sNota = oEvt.getSource().getValue().toString(); 
			// MessageBox.information(sNota, mSettings); 
			// var sNota = oEvt.getSource().getValue();
			// MessageBox.information(sNota.toString(), mSettings);
			var sNota = oEvt.getParameters().value.toString();
			MessageBox.information(sNota, mSettings);
			this._consolePrint(oEvt.getSource(), "value");
		},
		onScan: function (oEvt) {
			var oParameters = oEvt.getParameters();
			var sMessage;
			if (oParameters.format) {
				sMessage = oParameters.text + " " + oParameters.format;
			} else {
				sMessage = oParameters.text;
			}
			MessageBox.show(sMessage);
			console.log(sFormat);
		},
		_consolePrint: function (oManagedObject, sProperty) {
			var sPropertyValue = oManagedObject.getProperty(sProperty);
			console.log(sPropertyValue);
		},

		onBack: function (oEvent) {
			//This code was generated by the layout editor.
		}
	});
});